# vllm-gpu-base is built from upstream vLLM source in CI (arm64 only).
# See images/vllm-gpu-base/README.md and .github/workflows/push.yml.
FROM scratch
LABEL org.opencontainers.image.description="Placeholder — build via CI clone of vllm-project/vllm docker/Dockerfile"
